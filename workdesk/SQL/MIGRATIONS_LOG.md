# SQL Migrations Log

## Como usar
1. Cada mudanca no banco = 1 arquivo numerado em `migrations/`
2. Aplicar no Supabase SQL Editor na ordem
3. Marcar como aplicado aqui
4. Atualizar `backend/src/database/schema.sql` para refletir o estado final

## Regras
- NUNCA editar migrations ja aplicadas
- NUNCA editar schema.sql diretamente para mudancas novas (criar migration primeiro)
- Cada migration tem UP (aplicar) e DOWN (reverter) comentado
- Testar migration localmente antes de aplicar em producao

## Historico

| # | Arquivo | Descricao | Aplicado? |
|---|---------|-----------|-----------|
| 001 | 001_baseline.sql | Schema completo Fase 1 (referencia) | Sim (Fase 1) |
| 003 | 003_fix_db_inconsistencies.sql | Remove scan_frequency_hours duplicada, IVFFlat->HNSW, add search_permission config | Sim (2026-03-17) |
| 004 | 004_reports_table.sql | Tabela reports (dashboard compartilhavel) + indices analytics | Sim (Fase 4) |
| 005 | 005_city_groups.sql | Tabelas city_groups + city_group_members (agrupamento de cidades) | Sim (Fase 6, 2026-04-06) |
| 017 | 017_billing_history.sql | Tabela billing_history (fechamento mensal de custos) | Sim (Fase 5, 2026-04-06) |
| 018 | 018_city_groups.sql | Tabelas city_groups + city_group_members (agrupamento de cidades no feed) | Sim (Fase 6, 2026-04-15) |
| 019 | 019_news_add_estado.sql | ALTER news ADD COLUMN estado + index (cidade, estado). Fix de cidades homonimas | **Sim** (verificado no banco em 02/08 — o log dizia pendente por engano) |
| 020 | 020_news_drop_resumo_agregado.sql | DROP coluna resumo_agregado (dead feature, nunca foi populada) | **Sim** (verificado no banco em 02/08 — a coluna nao existe mais) |
| 021 | 021_manual_search_web_toggle.sql | Config `manual_search_web_enabled` (default false). Desliga o ramo web da busca manual | **NAO aplicada** — a chave nao existe no banco, entao o backend cai no default do codigo, que hoje e **`true`** (o ramo web foi RELIGADO ao migrar pro SERP API). Rodar esta migration DESLIGARIA o web — so rodar se for isso que se quer |
| 021b | 021_executive_cache.sql | Tabela executive_cache | **Sim** (verificado em 02/08) |
| 022 | 022_executive_cache_search_id.sql | Coluna search_id em executive_cache | **Sim** (verificado em 02/08) |
| 024 | 024_limpar_configs_mortas.sql | **DESTRUTIVA (DELETE)**, mas neutra: apaga 7 configs mortas. Todas existem no `DEFAULTS` hardcoded do configManager — inclusive na `main`, com os MESMOS valores do banco — entao o `get()` cai no default e producao nao muda. Bloco 3 (opcional, comentado) apaga `manual_search_max_results_30d` e assim resolve o conflito de semantica entre `main` (teto de coleta) e `develop` (teto de analise): cada codigo cai no seu default | **Pendente — o Joao vai rodar** |
| 025 | 025_rls_fechar_anon.sql | 🚨 **SEGURANCA.** Liga RLS em 13 tabelas. Medido em 02/08: a chave **anon** (publica — esta no APK e no bundle do admin) lia `user_profiles`, `news`, `search_cache`, `budget_tracking`, `system_config` e outras, e o INSERT chegava na validacao NOT NULL do Postgres (ou seja, **a escrita tambem passava**). Nao quebra nada: mobile e admin usam Supabase so pra `auth`, e o backend usa a service key, que ignora RLS | ✅ **Aplicada em 16/08** — verificado no banco: RLS `true` nas 12 tabelas restantes (a 13a, `user_favorites`, foi dropada pela 031 logo depois). Producao seguiu de pe: o backend usa a service key, que ignora RLS |
| 023 | 023_manual_search_teto_aberto.sql | Fase 8.2/8.3/8.4: `manual_search_max_results_30d` = **0 (sem teto)** e vira BASE — os outros periodos escalam dela por raiz quadrada, sem faixas. Insere `manual_search_horizon_days` (180) e `dedup_gpt_confirm_enabled` (false) | **OPCIONAL** — so mexe em `system_config`, e o painel admin faz o mesmo (o PATCH usa `configManager.set`, que e upsert). Preferir o painel |

| 026 | 026_rejected_urls_search_id.sql | ADD COLUMN `search_id` (nullable) + index parcial em `pipeline_rejected_urls`. Aditiva e reversivel, nao toca linha existente. Sem ela a busca manual **nao consegue** persistir por que rejeitou — a tabela so aceitava `location_id`, e busca manual roda em cidade fora de `monitored_locations`. Motivador: Goiania/30d levou 74 conteudos a 27 extracoes em 03/08 e a unica forma de saber o motivo era re-rodar o pipeline **pagando Jina + GPT** | ✅ **Aplicada em 04/08** |

| 027 | 027_openai_concorrencia.sql | `api_rate_limits.openai.max_concurrent` **5 -> 20** (e `min_time_ms` 200 -> 50). O 5 era chute conservador de fev/2026, nunca revisado, e e o gargalo mais apertado do sistema: estrangula o estagio 5 da busca manual (~6 min para 450 artigos, contra ~1,5 min com 20) e, por ser o mesmo Bottleneck, deixa o **auto-scan do cliente na fila** enquanto uma busca longa roda. Nao descarta nada — so aumenta vazao. ⚠️ Afeta producao na hora (banco compartilhado), mas so pra acelerar | ✅ **Aplicada em 04/08** — verificado no banco: `openai.max_concurrent` = 20 |

| 028 | 028_jina_concorrencia.sql | `api_rate_limits.jina.max_concurrent` **10 -> 20**. Medido em 03/08 (Goiania, 17 assuntos): o Jina levou **327s dos 515s** da busca — 63% do tempo. Sobe JUNTO com `manual_search_fetch_concurrency` (10 -> 20, no codigo): sao dois limitadores em serie e subir so um nao acelera nada. ⚠️ Pre-requisito ja feito no mesmo commit: o fetcher passou a tratar **429 com `Retry-After`** — antes, um 429 virava excecao fora da lista de fallback e o artigo era perdido em silencio | ✅ **Aplicada em 04/08** — verificado no banco: `jina.max_concurrent` = 20 |

| 030 | 030_news_hora_publicacao.sql | ADD COLUMN `hora_publicacao` (TIME, nullable) em `news`. **Corrige um campo que mentia em 100% dos itens**: a slug da materia carimba uma hora, e ela lia `data_ocorrencia`, que e coluna **DATE** — entao todo item do app, sempre, exibia `00:00`. Pego pelo Joao olhando o aparelho em 09/08. Passa a guardar a hora que o **veiculo publicou**, impressa na propria pagina; nao a do fato (quase nunca extraivel: "por volta das 3h") nem `created_at` (essa e a hora da varredura, que foi justamente o que ele apontou como inutil). `TIME` sem fuso **de proposito** — e a hora local que o portal imprimiu, e converter so pioraria; exibir cru. Nullable: linha antiga e artigo sem hora ficam sem, e o app **omite o carimbo** em vez de inventar 00:00. `data_ocorrencia` nao e tocada (DATE, indexada, usada em todo filtro). Aditiva e reversivel | ✅ **Aplicada em 09/08** — confirmada pelo Joao |

| 029 | 029_news_titulo.sql | ADD COLUMN `titulo` (TEXT, nullable) em `news`. O Filter2 passa a **escrever** uma manchete curta e neutra no mesmo request que ja extrai cidade/data/tipo — custo marginal ~0, nenhuma chamada nova. Motivador: o redesign "fio de agencia" tem a manchete como peca central, e ate aqui nao existia campo de titulo (o app usava o tipo de crime em caixa alta). Nao copia o titulo do veiculo **de proposito**: imprensa policial titula no sensacional e o produto e sobrio por tese. Nullable em duas frentes — linhas antigas ficam sem (o app compoe dos campos estruturados) e item novo sem headline **nao e rejeitado**. Aditiva e reversivel | ✅ **Aplicada em 09/08** — confirmada pelo Joao |

| 031 | 031_drop_user_favorites.sql | **DESTRUTIVA (DROP TABLE).** Remove `user_favorites`. A feature saiu do codigo inteiro na Fase D (11/08): a tela de favoritos do app era **orfa** — nenhuma rota, aba ou push a instanciava —, e o unico jeito de salvar era arrastar o card pra direita, gesto sem afordancia que saiu junto. Nada no banco aponta pra tabela (as duas FKs saem dela), entao o CASCADE so derruba o proprio indice. ⚠️ **ORDEM: 025 ANTES desta** — a 025 faz `ALTER TABLE user_favorites ENABLE ROW LEVEL SECURITY` e quebra se a tabela ja tiver sumido. ⚠️ Depois desta, replay das migrations do zero quebra na 010 (que faz TRUNCATE na tabela); aceito, o baseline util e o banco atual | ✅ **Aplicada em 16/08**, depois da promocao da `main` (`f6ef620`) e depois da 025 — verificado: a tabela nao existe mais |

| 032 | 032_preferencias_de_notificacao.sql | **Aditiva e reversivel.** Cria `user_notification_prefs` (cidades[], categorias[], estatisticas bool). Hoje o push vai pra **TODOS os aparelhos ativos, sem filtro nenhum** — o cliente de Florianopolis recebe push de Porto Alegre, e `natureza = 'estatistica'` ("homicidios cairam 12%") chega com a mesma urgencia de uma ocorrencia. Medido em 11/08 (21 dias): 30 noticias, media **2,0/dia**, pico 5, 4 aparelhos — **nao ha problema de volume, ha problema de relevancia**, e por isso a migration traz preferencia e nao traz digest. 🚨 `NULL` = todas, de proposito: quem ja tem aparelho registrado continua recebendo tudo ate abrir a tela pela primeira vez. **Migration nao pode calar ninguem em silencio** — quem para de receber alerta sem ter pedido nao reclama, acha que o produto quebrou | ✅ **Aplicada em 16/08** — verificado: `user_notification_prefs` existe |

| 033 | 033_relatorio_sem_expiracao.sql | **Aditiva e reversivel.** `reports.expires_at` perde o default de 30 dias e as linhas existentes passam a NULL. O prazo nunca apagou nada — **nenhuma rotina deleta linha de `reports`** —, ele so fazia `getReport()` recusar a partir do dia 31, em silencio: o cliente que arquivou o link via "Relatorio indisponivel" com a linha intacta no banco. Fazia sentido enquanto o relatorio era um link descartavel do painel; deixou de fazer na Fase E2, quando virou peca de apresentacao desenhada pra chegar no cliente do cliente. Custo de guardar: JSONB de ~20-60 KB por linha, dezenas por mes — alguns MB por ano, barato demais pra valer politica de expurgo. `NULL` em vez de data longe **de proposito**: data longe e a mesma armadilha adiada e mente pra quem le a tabela. A leitura em `analyticsQueries.ts` foi ajustada no mesmo commit. O `DROP INDEX idx_reports_expires` fica **comentado** — a migration nao precisa de verbo destrutivo pra resolver o problema | ✅ **Aplicada em 16/08**, por ultimo, depois de a `main` (`f6ef620`) ja estar no ar — verificado: default removido, **20 de 20 sem prazo, 0 vencidos**. Os 4 relatorios que estavam inalcancaveis voltaram a abrir |
| 034 | 034_news_corpo.sql | **Aditiva e reversivel.** `news.corpo` (TEXT, NULL): o texto de leitura que a FOLHA mostra no toque. Existe porque o card imprime o `resumo` INTEIRO (decisao de 09/08, e boa), entao a folha aberta mostrava o MESMO texto caractere por caractere — tocar num card nao entregava nenhuma palavra a mais. O teto de 190 do `resumo` e do CARD e nao pode subir sem quebrar o ritmo da lista, dai o campo separado. **Custo ~zero:** a Jina ja busca o artigo inteiro e o Filter2 ja le ate 6000 caracteres pra extrair cidade/data/tipo — o conteudo estava em maos e era descartado. ~900 caracteres a mais saem no MESMO request (~200 tokens de saida, ~US$ 0,0001 por materia), sem chamada nem fetch novos. ⚠️ Ficou mais necessario depois do dedup de 24/08: com varios relatos FUNDIDOS num texto so, a uniao de tres veiculos nao cabe em 190. NULL nas linhas antigas de proposito — a folha cai no `resumo`, que e o comportamento atual, e item novo sem corpo NAO e rejeitado | ✅ **Aplicada** — verificado no banco em 26/08: a coluna `news.corpo` existe, NULL nas 324 linhas (correto: so noticia nova recebe, e producao ainda roda o commit `5654361` de 17/08) |

| 035 | 035_rls_fechar_as_quatro_que_sobraram.sql | 🚨 **SEGURANCA.** Liga RLS em `reports`, `billing_history`, `city_groups`, `city_group_members` — as quatro que a 025 deixou abertas. Medido em 26/08 com a chave anon (e com caso de controle devolvendo `PGRST205`, entao a sonda vale): as quatro respondem 200 **com dados**, e o grant do role `anon` nelas e `SELECT,INSERT,UPDATE,DELETE,TRUNCATE`. Onde a RLS esta desligada nao ha trava nenhuma, e a chave anon vai dentro do APK. `reports` guarda o relatorio que o cliente manda pro cliente dele; `billing_history` entrega custo e breakdown. **Escaparam da 025 porque ela AFIRMA (linha 65) que `reports` ja estava fechada — nao estava** — e as outras tres nem sao citadas. Nao quebra nada, reverificado em 26/08 e nao herdado: mobile tem zero `.from(`, admin so usa `supabase.auth.*`, backend usa service key (bypass) | ✅ **Aplicada em 26/08 nos DOIS bancos** (staging primeiro, producao depois, autorizada pelo Joao). Verificado: RLS **19/19** nos dois; a chave anon devolve **0 linhas** nas quatro (controle `PGRST205` intacto); as contagens nao mudaram (`reports` 20, `billing_history` 4, `city_groups` 2, `city_group_members` 3) — RLS e acesso, nao apaga dado; producao seguiu de pe **sem restart** (uptime de 9 dias atravessou a migration) |

> ⚠️ Este log e preenchido a mao e ja desatualizou (019 e 020 estavam marcadas
> como pendentes e ja tinham sido rodadas). Antes de confiar nele, rodar
> **`npx tsx scripts/diagnostico-banco.ts`** — ele olha o estado REAL do banco
> (colunas, tabelas e configs) e e so leitura.

## Alteracoes manuais (sem migration file)

| Data | Descricao | Aplicado? |
|------|-----------|-----------|
| 2026-04-14 | ALTER TABLE search_cache: adicionou 'cancelled' no status check constraint | Sim |
| 2026-04-14 | TRUNCATE todas as tabelas de dados (news, logs, budget, search, devices, favorites, read) — reset pra testes | Sim |
